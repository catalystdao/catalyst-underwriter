export const underwriteTest = describe('Testing Underwrite', () => {
  it('should perform an underwrite', async () => {
    // const swap: Swap = {
    //   blockNumber,
    //   sendAsset,
    //   delay: 0,
    // };
    // const chain: Chain = { ...CHAINS[0], rpc: 'http://localhost:8545' };
    // const tx = await underwrite(swap, chain);
    // expect(tx).toBeTruthy();
  });
});
